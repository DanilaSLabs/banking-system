# Banking System (Банковская система на C++ / JSON)

Проект представляет собой учебную консольную банковскую систему,
написанную на **C++17** с хранением данных в формате **JSON**
(библиотека [nlohmann/json](https://github.com/nlohmann/json)).

Система позволяет создавать клиентов, открывать банковские счета,
проводить операции, начислять проценты и восстанавливать пароль.

---

## 🛠 Технологии
- C++17
- nlohmann::json (single-header)
- Xcode / g++
- Работа с файловой системой через `<filesystem>`

---

## 📂 Структура проекта
BankingSystem/
├── data/
│ └── database.json ← основная база данных
├── include/
│ ├── Account.h
│ ├── Customer.h
│ ├── DatabaseManager.h
│ └── nlohmann/
│ └── json.hpp ← библиотека JSON
├── scr/
│ ├── Account.cpp
│ ├── Customer.cpp
│ ├── DatabaseManager.cpp
│ └── main.cpp
└── docs/
├── README.md
├── ARCHITECTURE.md
├── FLOW.md
├── FEATURES.md
├── DB_FORMAT.md
├── TESTING.md
└── FUTURE.md

---

## ⚙️ Запуск
### Через терминал:
```bash
g++ -std=gnu++17 scr/*.cpp -Iinclude -o bank_app
./bank_app

 Через Xcode:

 Создать таргет Command Line Tool.

 Добавить все .cpp в раздел Compile Sources.

 Указать путь $(PROJECT_DIR)/BankingSystem/include в Header Search Paths.

 💾 База данных

 База хранится в data/database.json.
 При каждом сохранении создаются:

 database.json.tmp — временный файл для атомарной записи;

 database.json.bak — резервная копия предыдущей версии.

 🔐 Безопасность

 Нельзя перезаписать непустую базу пустыми данными;

 Все операции с JSON выполняются с проверкой ошибок;

 secretWord хранится в открытом виде, в будущем будет заменено на хеширование.

 ✨ Возможности

 Регистрация клиента;

 Выбор количества счетов:

 1 → Checking;

 2 → Checking + Savings;

 Логин с проверкой пароля;

 Начисление процентов по Savings при входе;

 Депозит, снятие, перевод, временные вклады;

 Восстановление пароля через email;

 Генерация уникальных ID счетов;

 Работа с JSON без потери данных.

 🧠 Автор и версия

 Автор: DanilaS

 Версия: 1.0

 Год: 2025
